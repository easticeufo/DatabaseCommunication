远程操作数据库协议：
基于TCP协议。
客户端请求格式：
Head+Body，Head为固定长度结构体：
struct Head
{
	UINT32 type; // 请求类型：1-插入 2-更新 3-删除
	UINT32 length; // body数据长度
	UINT8 authToken[32]; // 认证信息，body内容＋password的md5加密值
}

Body为可变长度，长度由Head.length确定，Body内容格式由Head.type确定，type=1或2或3时，body都为要执行的sql语句

服务器返回数据格式：
struct Result
{
	INT32 code; // 返回码：负数表示失败，正数表示成功
	char info[256]; // 附加内容，长度固定为256字节
}
